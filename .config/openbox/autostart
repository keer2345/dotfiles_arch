#
# These things are run when an Openbox X Session is started.
# You may place a similar script in $HOME/.config/openbox/autostart
# to run user-specific things.
#

# If you want to use GNOME config tools...
#
#if test -x /usr/lib/openbox/gnome-settings-daemon >/dev/null; then
#  /usr/lib/openbox/gnome-settings-daemon &
#elif which gnome-settings-daemon >/dev/null 2>&1; then
#  gnome-settings-daemon &
#fi

# If you want to use XFCE config tools...
#
#xfce-mcs-manager &

#!/bin/sh

xrandr --output eDP-1 --mode 1920x1200 --scale 1.0x1.0 --pos 0x0 --rate 90

# Set a background wallpaper
nitrogen --restore &

xfce4-power-manager &

# Launch a system tray
tint2 &

xset b off &

# Start a compositor for visual effects (e.g., shadows, transparency)
picom -b &

# 启动 GNOME Keyring Daemon 并导出环境变量
# 这是最关键的一步！
eval $(gnome-keyring-daemon --start --components=secrets)
export SSH_AUTH_SOCK
export GNOME_KEYRING_CONTROL

# 钥匙环，不需要每次都输入WIFI密码
/usr/bin/lxqt-policykit-agent &
# Start a network manager applet
nm-applet &

# Start a screen locker
xautolock -time 10 -locker slock &

# 启动通知守护进程
dunst &

# Start a volume control applet
# volumeicon &


# Launch Fcitx5 Input Method Framework
fcitx5 &

